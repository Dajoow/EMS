# Hdu_EMS_H750 项目记忆

## 1. 项目定位
- 项目名称：`BSMU_H750IB`
- 目标芯片：`STM32H750IBTx`（Cortex-M7）
- 主要用途：电池簇（BCMU）数据采集、聚合、本地/云端上报、TouchGFX 本地显示
- 工程形态：STM32CubeMX + FreeRTOS + LwIP + mbedTLS + TouchGFX + FDCAN + 外部 SDRAM

## 2. 根目录关键入口
- 主工程配置：`BSMU_H750IB.ioc`
- Keil 工程：`MDK-ARM/BSMU_H750IB.uvprojx`
- 主流程入口：`Core/Src/main.c`
- RTOS 线程编排：`Core/Src/freertos.c`
- 业务核心：
  - CAN 轮询/接收：`Core/Src/CAN_Control.c`
  - 电站数据聚合：`Core/Src/station_ctl.c`
  - 本地 TCP 客户端上报：`Core/Src/client.c`
  - HTTPS 云端上报：`Core/Src/http_client.c`
  - 持久化：`Core/Src/data_persistence.c`
- 网络参数：`LWIP/Target/lwipopts.h`

## 3. 启动与运行主链路（按代码实际顺序）
1. `main()` 启动后设置 `VTOR=0x90000000`，开启 I/D Cache，完成 HAL 与 MPU 初始化。
2. 读取 AT24Cxx EEPROM 配置到 `bsmuSetting`。
3. 初始化外设（GPIO、DMA、FDCAN2、FMC、DMA2D、LTDC、SPI/UART、RNG、RTC 等）。
4. 初始化 mbedTLS 与 TouchGFX（TouchGFX 分支内有手工 `portENABLE_INTERRUPTS()` 修复点）。
5. `MX_FREERTOS_Init()` 创建 `startup_Thread` 与 `TouchGFX` 线程，随后 `osKernelStart()`。
6. `Startup()` 中按顺序启动业务：
   - `StationDataInit()`
   - `flash_save_init()`（EasyFlash）
   - `mbedtls_net_init(NULL)`、`sntp_client_init()`
   - `BSMU_CANInit()`（CAN 收发任务）
   - `Module4G_Init()`
   - `screen_sleep_init()`、`modbus_init()`
   - 按 `bsmuSetting.local_flag` 启动本地 TCP 客户端 `ClientInit()`
   - 按 `bsmuSetting.yunduan_flag` 启动 HTTPS 任务 `http_client_init()`
   - 创建 `CPU_Task` 做运行态监控

## 4. 数据与通信架构

### 4.1 CAN 侧（BCMU -> BSMU）
- 电池簇数量：`cluster_num=20`（`Core/Inc/station_ctl.h`）
- `CAN_Control.c` 中建立：
  - `CANQueueHandle[20]`
  - `CAN_Rev_Task` + `CAN_Poll_Task`
  - `ETHSndSemHandle`（本地 TCP 发送触发）
  - `http_snd_sem_handle`（云端 HTTPS 发送触发）
- 使用 FDCAN FD 帧、64-byte DLC、CRC16(Modbus) 做分包/重组与校验。

### 4.2 本地 TCP 上报链路
- 代码：`Core/Src/client.c`
- TCP 连接参数来源于 EEPROM 的 `bsmuSetting`（IP/端口）。
- 发包逻辑：等待 `ETHSndSemHandle`，循环发送在线簇 `Client_Sd_t`，再发 `Client_Sd_Station_t`。

### 4.3 HTTPS 云端上报链路
- 代码：`Core/Src/http_client.c`
- 关键宏：
  - `HOST = "hdzbdzbms.online"`
  - `HTTPS_PORT = "443"`
  - `API_STATISTICS = /api/bmsRequest/sendSum`
  - `API_CLUSTERS = /api/bmsRequest/clusterData`
- 发送内容：
  - 站级汇总 JSON（状态、电压、电流、SOC、SOH、功率等）
  - 簇级 JSON（簇状态 + 多组数组经 base64 编码）
- TLS：mbedTLS + 证书解析 + SSL 握手；证书校验模式为 `MBEDTLS_SSL_VERIFY_OPTIONAL`（当前配置）。

## 5. 内存与数据布局（关键）
- SDRAM/静态区地址宏（`Core/Inc/main.h`）：
  - `ETHSendBuffAdd      = 0xC0384000`
  - `CANRevBuffStartAdd  = 0xC03B0000`
  - `CANSndBuffStartAdd  = 0xC03F8000`
- 显式映射数据：
  - `Client_Sd_t Client_Sd[20]` 放置到 `.ETHSendBuff`/指定地址
  - `FDCAN_REV_Buff`、`FDCAN_SND_Buff` 放置到指定 section/地址
  - `httpc_clusters` 映射到 `0xC0400000`
  - `httpc_station_statistics` 映射到 `0xC040E6A0`
- LwIP 堆指针：`LWIP_RAM_HEAP_POINTER = 0x30020000`（`lwipopts.h`）
- CODE_FLASH:25Q64FV
- DATA_FLASH:25Q256FV

## 6. 持久化与配置
- EEPROM（AT24Cxx）用于运行配置读取（如本地/云端开关、IP/端口等）。
- EasyFlash 用于阈值与统计累计：
  - `cluster_falsh`（原始 key 拼写即如此）
  - `modbus_threshold_flash`
- `flash_save` 线程每 30 分钟刷写一次簇累计数据。

## 7. 线程与调试观察点
- `freertos.c` 中 `CPU_Task` 周期打印：
  - 任务列表、运行时统计、最小剩余栈、Heap 水位
  - 每个 `CANQueue` 的消息数与剩余空间
- `TOUCHGFX_ENABLE` / `CPU_PRINTF` 位于 `Core/Inc/main.h`。

## 8. 当前工程状态与风险提示
- 工作区检测到已有未提交变更：`MDK-ARM/RTE/RTE_Components.h`（删除状态，非本次生成所致）。
- 关注风险点：
  - `http_client.c` 中 `SERVER_TOKEN` 明文宏定义，属于敏感配置，后续建议转外部安全注入。
  - TLS 校验当前为 `VERIFY_OPTIONAL`，上线环境建议评估收紧。
  - 多处固定地址映射与 Cache/MPU 强相关，改动内存布局前需先核对 linker/section 与 DMA 一致性。
  - README 中保留历史调试结论与手工修复点（如 TouchGFX 中断、ETH 热插拔），CubeMX 重新生成后需复核。

## 9. 新人接手最小路径
1. 先看 `Core/Src/main.c` + `Core/Src/freertos.c`，理解启动与线程创建顺序。
2. 再看 `Core/Src/CAN_Control.c` + `Core/Src/station_ctl.c`，掌握采集与聚合。
3. 根据场景看上报链路：
   - 本地 TCP：`Core/Src/client.c`
   - 云端 HTTPS：`Core/Src/http_client.c`
4. 调网路问题先看 `LWIP/Target/lwipopts.h` 和 DHCP/SNTP 相关初始化。
5. 调阈值与历史统计先看 `data_persistence.*` 与 `modbus_threshold_flash_t`。

## 10. 可直接复用的排查命令（PowerShell）
```powershell
# 查看工作区状态（避免覆盖他人改动）
git status --short

# 快速定位启动主流程
rg -n "int main|MX_FREERTOS_Init|osKernelStart|Startup" Core/Src/main.c Core/Src/freertos.c

# 快速定位云端上报关键宏与接口
rg -n "HOST|HTTPS_PORT|API_STATISTICS|API_CLUSTERS|SERVER_TOKEN|HTTPC_ENABLED" Core/Src/http_client.c

# 快速定位内存映射宏
rg -n "ETHSendBuffAdd|CANRevBuffStartAdd|CANSndBuffStartAdd" Core/Inc/main.h
```

---
生成时间：2026-05-28（Asia/Shanghai）  
生成方式：只读扫描工程后整理（未修改现有源码，仅新增本文件）
