# Hdu_EMS_H750 项目记忆

## 1. 项目定位
- 项目名称：`BSMU_H750IB`
- 目标芯片：`STM32H750IBTx`（Cortex-M7）
- 主要用途：电池簇（BCMU）数据采集、聚合、本地/云端上报、TouchGFX 本地显示
- 工程形态：STM32CubeMX + FreeRTOS + LwIP + mbedTLS + TouchGFX + FDCAN + 外部 SDRAM

## 2. 根目录关键入口
- 主工程配置：`BSMU_H750IB.ioc`
- Keil 工程：`MDK-ARM/BSMU_H750IB.uvprojx`
- 主流程入口：`Core/Src/main.c`
- RTOS 线程编排：`Core/Src/freertos.c`
- 业务核心：
  - CAN 轮询/接收：`Core/Src/CAN_Control.c`
  - 电站数据聚合：`Core/Src/station_ctl.c`
  - 本地 TCP 客户端上报：`Core/Src/client.c`
  - HTTPS 云端上报：`Core/Src/http_client.c`
  - 持久化：`Core/Src/data_persistence.c`
- 网络参数：`LWIP/Target/lwipopts.h`

## 3. 启动与运行主链路（按代码实际顺序）
1. `main()` 启动后设置 `VTOR=0x90000000`，开启 I/D Cache，完成 HAL 与 MPU 初始化。
2. 读取 AT24Cxx EEPROM 配置到 `bsmuSetting`。
3. 初始化外设（GPIO、DMA、FDCAN2、FMC、DMA2D、LTDC、SPI/UART、RNG、RTC 等）。
4. 初始化 mbedTLS 与 TouchGFX（TouchGFX 分支内有手工 `portENABLE_INTERRUPTS()` 修复点）。
5. `MX_FREERTOS_Init()` 创建 `startup_Thread` 与 `TouchGFX` 线程，随后 `osKernelStart()`。
6. `Startup()` 中按顺序启动业务：
   - `StationDataInit()`
   - `flash_save_init()`（EasyFlash）
   - `mbedtls_net_init(NULL)`、`sntp_client_init()`
   - `BSMU_CANInit()`（CAN 收发任务）
   - `Module4G_Init()`
   - `screen_sleep_init()`、`modbus_init()`
   - 按 `bsmuSetting.local_flag` 启动本地 TCP 客户端 `ClientInit()`
   - 按 `bsmuSetting.yunduan_flag` 启动 HTTPS 任务 `http_client_init()`
   - 创建 `CPU_Task` 做运行态监控

## 4. 数据与通信架构

### 4.1 CAN 侧（BCMU -> BSMU）
- 电池簇数量：`cluster_num=20`（`Core/Inc/station_ctl.h`）
- `CAN_Control.c` 中建立：
  - `CANQueueHandle[20]`
  - `CAN_Rev_Task` + `CAN_Poll_Task`
  - `ETHSndSemHandle`（本地 TCP 发送触发）
  - `http_snd_sem_handle`（云端 HTTPS 发送触发）
- 使用 FDCAN FD 帧、64-byte DLC、CRC16(Modbus) 做分包/重组与校验。

### 4.2 本地 TCP 上报链路
- 代码：`Core/Src/client.c`
- TCP 连接参数来源于 EEPROM 的 `bsmuSetting`（IP/端口）。
- 发包逻辑：等待 `ETHSndSemHandle`，循环发送在线簇 `Client_Sd_t`，再发 `Client_Sd_Station_t`。

### 4.3 HTTPS 云端上报链路
- 代码：`Core/Src/http_client.c`
- 关键宏：
  - `HOST = "hdzbdzbms.online"`
  - `HTTPS_PORT = "443"`
  - `API_STATISTICS = /api/bmsRequest/sendSum`
  - `API_CLUSTERS = /api/bmsRequest/clusterData`
- 发送内容：
  - 站级汇总 JSON（状态、电压、电流、SOC、SOH、功率等）
  - 簇级 JSON（簇状态 + 多组数组经 base64 编码）
- TLS：mbedTLS + 证书解析 + SSL 握手；证书校验模式为 `MBEDTLS_SSL_VERIFY_OPTIONAL`（当前配置）。

## 5. 内存与数据布局（关键）
- SDRAM/静态区地址宏（`Core/Inc/main.h`）：
  - `ETHSendBuffAdd      = 0xC0384000`
  - `CANRevBuffStartAdd  = 0xC03B0000`
  - `CANSndBuffStartAdd  = 0xC03F8000`
- 显式映射数据：
  - `Client_Sd_t Client_Sd[20]` 放置到 `.ETHSendBuff`/指定地址
  - `FDCAN_REV_Buff`、`FDCAN_SND_Buff` 放置到指定 section/地址
  - `httpc_clusters` 映射到 `0xC0400000`
  - `httpc_station_statistics` 映射到 `0xC040E6A0`
- LwIP 堆指针：`LWIP_RAM_HEAP_POINTER = 0x30020000`（`lwipopts.h`）
- CODE_FLASH:25Q64FV
- DATA_FLASH:25Q256FV

## 6. 持久化与配置
- EEPROM（AT24Cxx）用于运行配置读取（如本地/云端开关、IP/端口等）。
- EasyFlash 用于阈值与统计累计：
  - `cluster_falsh`（原始 key 拼写即如此）
  - `modbus_threshold_flash`
- `flash_save` 线程每 30 分钟刷写一次簇累计数据。

## 7. 线程与调试观察点
- `freertos.c` 中 `CPU_Task` 周期打印：
  - 任务列表、运行时统计、最小剩余栈、Heap 水位
  - 每个 `CANQueue` 的消息数与剩余空间
- `TOUCHGFX_ENABLE` / `CPU_PRINTF` 位于 `Core/Inc/main.h`。

## 8. 当前工程状态与风险提示
- 工作区检测到已有未提交变更：`MDK-ARM/RTE/RTE_Components.h`（删除状态，非本次生成所致）。
- 关注风险点：
  - `http_client.c` 中 `SERVER_TOKEN` 明文宏定义，属于敏感配置，后续建议转外部安全注入。
  - TLS 校验当前为 `VERIFY_OPTIONAL`，上线环境建议评估收紧。
  - 多处固定地址映射与 Cache/MPU 强相关，改动内存布局前需先核对 linker/section 与 DMA 一致性。
  - README 中保留历史调试结论与手工修复点（如 TouchGFX 中断、ETH 热插拔），CubeMX 重新生成后需复核。

## 9. 新人接手最小路径
1. 先看 `Core/Src/main.c` + `Core/Src/freertos.c`，理解启动与线程创建顺序。
2. 再看 `Core/Src/CAN_Control.c` + `Core/Src/station_ctl.c`，掌握采集与聚合。
3. 根据场景看上报链路：
   - 本地 TCP：`Core/Src/client.c`
   - 云端 HTTPS：`Core/Src/http_client.c`
4. 调网路问题先看 `LWIP/Target/lwipopts.h` 和 DHCP/SNTP 相关初始化。
5. 调阈值与历史统计先看 `data_persistence.*` 与 `modbus_threshold_flash_t`。

## 10. 可直接复用的排查命令（PowerShell）
```powershell
# 查看工作区状态（避免覆盖他人改动）
git status --short

# 快速定位启动主流程
rg -n "int main|MX_FREERTOS_Init|osKernelStart|Startup" Core/Src/main.c Core/Src/freertos.c

# 快速定位云端上报关键宏与接口
rg -n "HOST|HTTPS_PORT|API_STATISTICS|API_CLUSTERS|SERVER_TOKEN|HTTPC_ENABLED" Core/Src/http_client.c

# 快速定位内存映射宏
rg -n "ETHSendBuffAdd|CANRevBuffStartAdd|CANSndBuffStartAdd" Core/Inc/main.h
```

---
生成时间：2026-05-28（Asia/Shanghai）  
生成方式：只读扫描工程后整理（未修改现有源码，仅新增本文件）

---

## 11. 2026-06-03 电池堆结构调整日志

### 11.1 目标拓扑
- 本次将工程运行结构从历史多簇/多组形态收敛为：`1 BSMU -> 1 BCMU -> 2 BMU`。
- 核心数量宏位于 `Core/Inc/station_ctl.h`：
  - `cluster_num = 1`
  - `GRP_num = 2`
  - `GRP_BAT_num = 12`
  - `TOTOL_BAT_num = GRP_num * GRP_BAT_num`

### 11.2 主要代码变更
- `Core/Src/CAN_Control.c`
  - `BCMU_ID` 收敛为 1 个 BCMU。
  - `ReturnBCMU_Mail()` 只保留 BCMU1 有效返回。
  - 开关控制、CAN 轮询、BMU 在线状态解析改为使用 `cluster_num` / `GRP_num`。
  - CAN 队列创建收敛为当前 1 簇需要的队列。
- `Drivers/BSP/Components/RS485/modbus_data.c`
  - 引入 `station_ctl.h`，统一使用 `cluster_num` / `TOTOL_BAT_num`。
  - Modbus 簇数组、单体电压/温度/SOC/SOH/内阻数组由固定 `[20][360]` 改为拓扑宏控制。
  - Modbus 浮点区和 bit 区的结束地址按 `TOTOL_BAT_num` 动态计算。
- `Core/Src/station_ctl.c`
  - 外部 Modbus 数据声明与本地统计数组改为 `cluster_num` / `TOTOL_BAT_num`。
  - `CalStationData()` 增加 `OnlineNUM > 0` 保护，避免单簇离线时除 0。
- `Core/Src/http_client.c`
  - 云端簇数量使用 `cluster_num`。
- `TouchGFX/gui/src/model/Model.cpp`
  - Model 层读取 `Client_Sd` 前夹紧 `BCMU_SEL` / `BMU_SEL`，避免旧状态残留导致数组越界。
- `TouchGFX/gui/src/mainscreen_screen/MainScreenView.cpp`
  - BCMU2-20 点击函数增加运行期禁用保护。
  - BMU3-30 点击函数增加运行期禁用保护。
  - `BCMU1_clicked()` / 在线刷新循环改为只刷新 `GRP_num` 个 BMU。
  - `NotifyViewMsg()` 入口夹紧当前选择值。
  - `balance_state_update()` 修正 `Client_Sd` 下标，从 `BCMU_SEL` 改为 `BCMU_SEL - 1`，并对 BMU 编号做范围检查。
  - 清理旧 BCMU2-20 自动选择注释块残留的多余 `}`。
  - 将部分告警标题格式串改为 ASCII，避免 Keil ARMCC 因源文件编码解析中文字符串时报 `missing closing quote`。
- `TouchGFX/gui/src/setting_screen/SettingView.cpp`
  - 设置界面保存“簇装机数/cu_num”时夹紧到 `1 ~ cluster_num`。

### 11.3 编译验证
- Keil 工程：`BSMU_H750IB`
- 编译器：ARM Compiler V5.06 update 7 build 960
- 结果：2026-06-03 晚间 Rebuild 已通过。
- 剩余 warning 多数为历史遗留项，包括：
  - `FreeRTOSConfig.h` 宏重复定义。
  - `http_client.c` 中 goto 绕过初始化、指针类型不匹配、未使用变量。
  - `modbus_data.c` 对 `void *` 做指针运算的 warning。
  - TouchGFX/mbedTLS/FreeRTOS 第三方或生成代码 warning。

### 11.4 后续注意
- `TouchGFX/gui/src/mainscreen_screen/MainScreenView.cpp` 仍保留 20 个 BCMU 按钮和 30 个 BMU 按钮的界面对象，本次采用点击保护禁用超出拓扑范围的对象，未重做 TouchGFX 画面。
- `MDK-ARM/BSMU_H750IB.uvprojx` / `MDK-ARM/BSMU_H750IB.uvoptx` 在本次 Keil 构建过程中也发生较大变更，提交前需确认这些工程文件变更是否属于用户希望保留的工程配置。
- `MainScreenView.cpp` 当前部分中文注释在终端中显示为乱码，但 Keil 已能编译；后续如要恢复中文显示，建议统一确认源文件编码后再批量处理。

记录时间：2026-06-03 22:59（Asia/Shanghai）

---

## 12. 2026-06-04 TouchGFX GUI 调试日志

### 12.1 本次界面目标
- 当前工程拓扑已经收敛为 `1 BSMU -> 1 BCMU -> 2 BMU`。
- TouchGFX 主界面第一步先适配 BCMU 选择区：
  - 保留 `BCMU1`。
  - 在 TouchGFX Designer 中将 `BCMU2` 到 `BCMU20` 设置为不可见。
  - 暂不删除这些控件，避免生成代码后因旧回调、旧数组引用导致编译连锁错误。

### 12.2 已处理的 TouchGFX 问题

#### 问题 1：Generate Assets 找不到 imageconvert.out
- 现象：
  - TouchGFX Designer 执行 `Generate Code` 时，`Generate` 成功，但 `Generate Assets` 失败。
  - 报错关键字：
    - `imageconvert.out`
    - `make (e=2): 系统找不到指定的文件`
- 原因：
  - 工程配置最初指向 `../Middlewares/ST/touchgfx`。
  - 本工程内置的 TouchGFX 中间件工具/模拟器库不完整，不能稳定支撑 PC simulator 的资源转换和链接。
- 修复：
  - 后续将 TouchGFX 路径统一改为本机完整安装目录：
    - `C:/TouchGFX/4.21.4/touchgfx`
    - `C:/TouchGFX/4.21.4/env`

#### 问题 2：Run Simulator 编译找不到 cluster_num / GRP_num
- 现象：
  - `Run Simulator` 编译 `MainScreenView.cpp` / `SettingView.cpp` 失败。
  - 报错关键字：
    - `'cluster_num' was not declared in this scope`
    - `'GRP_num' was not declared in this scope`
- 原因：
  - 硬件工程中 `cluster_num` / `GRP_num` 定义在 `Core/Inc/station_ctl.h`。
  - 模拟器编译路径与硬件编译路径不同，部分硬件头文件被 `#ifndef SIMULATOR` 隔离，导致 UI 代码在 PC simulator 下看不到拓扑宏。
- 修复：
  - 在 `TouchGFX/config/gcc/app.mk` 的 `user_cflags` 中增加当前拓扑宏：
    - `-Dcluster_num=1`
    - `-DGRP_num=2`
- 后续注意：
  - 如果后续拓扑数量再次变化，模拟器宏也要同步更新，否则 TouchGFX simulator 与硬件工程会不一致。

#### 问题 3：Run Simulator 链接找不到 libtouchgfx / jpeg-8
- 现象：
  - 源码已编译通过，但链接 `build/bin/simulator.exe` 失败。
  - 报错关键字：
    - `cannot find -ltouchgfx`
    - `cannot find -ljpeg-8`
- 原因：
  - `TouchGFX/config/gcc/app.mk` 只改一次不够。
  - TouchGFX Designer 会根据 `.touchgfx` / `target.config` 重新生成配置，把路径改回 `../Middlewares/ST/touchgfx`。
  - 工程内置 `Middlewares/ST/touchgfx` 缺少 MinGW simulator 需要的 `lib/win/mingw32/libtouchgfx.a` 与 libjpeg 相关库。
- 修复：
  - 将以下配置源头同步改为完整 TouchGFX 安装路径：
    - `TouchGFX/BSMU_H750IB.touchgfx`
    - `TouchGFX/ApplicationTemplate.touchgfx.part`
    - `TouchGFX/target.config`
    - `TouchGFX/config/gcc/app.mk`
    - `TouchGFX/config/msvs/Application.props`
- 验证：
  - 使用 TouchGFX 自带 MinGW make 清理并重新构建 simulator。
  - `Linking build/bin/simulator.exe` 已通过。

### 12.3 当前可用操作路径
- 用 TouchGFX Designer 打开：
  - `TouchGFX/BSMU_H750IB.touchgfx`
- 修改界面后先执行：
  - `Generate Code`
  - `Run Simulator`
- 如果 Designer 又出现路径被改回 `../Middlewares/ST/touchgfx` 的情况，优先检查：
  - `TouchGFX/BSMU_H750IB.touchgfx`
  - `TouchGFX/target.config`
  - `TouchGFX/config/gcc/app.mk`
- 不要只改 `app.mk`，因为它可能被 Designer 重新生成覆盖。

### 12.4 本次遗留注意
- `BCMU2` 到 `BCMU20` 当前是隐藏，不是删除。
- 后续改 BMU 结构时，同样建议先隐藏/禁用多余 BMU 控件，再确认生成代码和模拟器可运行，最后再考虑清理 C++ 回调与生成对象。
- 本次运行 `make clean` 可能影响 `TouchGFX/generated/images/src/.idea/*` 这类生成目录下的已跟踪文件，提交前需要检查是否保留这些删除。

### 12.5 显示验证结果
- TouchGFX 主界面已完成当前拓扑的第一版显示适配：
  - `BCMU1` 保留显示。
  - `BCMU2` 到 `BCMU20` 已隐藏。
  - `BMU1`、`BMU2` 保留显示。
  - `BMU3` 到 `BMU30` 已隐藏。
- 运行时在线/离线逻辑仍保留：
  - BCMU 在线时显示 `BCMU1`，离线时显示 `/`。
  - BMU 在线时显示 `BMU1` / `BMU2`，离线时显示 `/`。
- 用户已确认新界面可以正常显示。
- 后续如果继续重构 UI，优先保持 `1 BCMU -> 2 BMU -> 每 BMU 12 Cell` 的界面语义，再逐步清理历史 20 BCMU / 30 BMU 的回调代码。

记录时间：2026-06-04（Asia/Shanghai）

## 13. 近期更新记录（2026-07-16）

### 13.1 当前电池拓扑与工艺依据
- 当前代码拓扑已经调整为：
  - `cluster_num=1`：1 个 BCMU/电池簇。
  - `GRP_num=2`：每簇 2 个 BMU。
  - `GRP_BAT_num=12`：每个 BMU 管理 12 节电池。
  - 合计 24 节 2V/500Ah 铅炭电池。
- EMS 策略参考文件：`分布式基站储备系统用-铅炭电池2V500Ah运行工艺标准.pdf`（Q/CY 3363.01-2025）。
- 已确认的关键工艺参数：
  - 常规放电深度不超过 70% DoD，即累计放电量不超过 350Ah。
  - 常规放电截止：组压 46.32V；单体保护值按表格采用 1.90V。
  - 备电放电允许 100% DoD，最大 500Ah；组压 43.20V；单体保护 1.70V。
  - 均充基准 2.45V/Cell，对应 24 节组压 58.8V。
  - 浮充基准 2.30V/Cell，对应组压 55.2V。
  - 完全充电滑流基准 2.50V/Cell，对应组压 60.0V。
  - 充电电流：0.35C=175A、0.10C=50A、0.03C=15A、0.015C=7.5A。
  - 单体充电保护 2.65V，电池温度保护 45℃。
  - 维护周期原则为连续 6 天常规运行后进行 1 天完全充电（6+1）。

### 13.2 EMS 策略初版
- 新增文件：
  - `Core/Inc/EMS_strategy.h`
  - `Core/Src/EMS_strategy.c`
- 初版策略已实现：
  - 停止、常规充电、完全充电、削峰放电、备电放电请求。
  - CC、CV、浮充、完全充电滑流、放电、故障锁定状态。
  - 单体过压、单体欠压、高温、SOC 上下限、BMS 离线和数据无效保护。
  - 充放电 Ah 累计、充电因子计算和温度补偿电压计算。
  - 故障锁定后保持输出关闭，通过 `EMS_ResetFault()` 人工复位。
- 当前完成度边界：
  - `EMS_strategy.c` 已加入 Keil 工程并能通过 ARMCC5 编译。
  - `freertos.c` 目前只包含 `EMS_strategy.h`，尚未创建 `EMS_Task`，也没有周期调用 `EMS_Step()`。
  - 策略输出尚未接入真实功率设备，不会控制现场充放电硬件。
  - Ah 累计值和 6+1 周期计数尚未接入 EasyFlash 持久化。

### 13.3 编译与代码质量治理
- Keil/ARMCC5 工程已恢复 TouchGFX 本地依赖路径：
  - Framework include：`../Middlewares/ST/touchgfx/framework/include`
  - Core library：`../Middlewares/ST/touchgfx/lib/core/cortex_m7/Keil/touchgfx_core.lib`
- EMS 源文件已移动到 `Core/Src`，头文件位于 `Core/Inc`，避免依赖 `MDK-ARM` 私有搜索路径。
- 对主要编译警告进行了治理：
  - 清理 `FreeRTOSConfig.h` 中重复的运行统计和断言宏。
  - 补充 `memset`、`modbus_init`、`TaskTcbPrintf` 等函数原型。
  - 修正 CMSIS-RTOS 任务入口函数签名。
  - 将 `Debug_printf`、`cmb_printf` 的格式串参数改为 `const char *`。
  - 修正 HTTPS 读写缓冲区的 `unsigned char *` 类型。
  - 修正 HTTP 请求头宏、cJSON 变量初始化和 Base64/cJSON 参数类型。
  - 修正 Modbus 对 `void *` 做地址运算的问题，并按寄存器起始地址计算偏移。
  - 修正 SFUD/HAL 枚举返回值混用和缺失返回类型等问题。
- 编译警告从最初 488 个逐步降低到 13 个；最近一次确认结果为：
  - `0 Error(s), 13 Warning(s)`。
  - 剩余项主要为未使用函数/变量、cJSON 的 GCC pragma 和 TouchGFX 未引用段提示。
- 板级验证：修改后的工程已完成上电测试，当前上电正常。

### 13.4 新功率架构确认
- 最新硬件关系调整为：

```text
市电 -> AC/DC -> 直流母线 -> 负载
                    ^
电池组 <-> 双向 DC/DC
             ^
            EMS <-> BMS
```

- 架构影响：
  - EMS 的实际控制对象从原规划中的 PCS 改为双向 DC/DC。
  - AC/DC 在市电正常时维持直流母线并向负载供电。
  - DC/DC 负责电池充电、削峰放电和市电故障时的备电放电。
  - 市电失效/AC/DC 故障应具有高于普通远程命令的本地备电优先级。
  - 新策略必须增加市电状态、AC/DC 状态、DC/DC 状态、母线电压和负载状态输入。
  - 输出模型需要从通用 `direction + target_current/voltage` 明确为 DC/DC 启停、充放电模式、电池电流限制、电池电压限制、母线目标电压和功率限制。
- 本次决定：暂不继续修改 EMS 源码，先冻结当前可上电版本。

### 13.5 下一步输入与待办
- 在重构 EMS/DC-DC 控制代码前，需要取得：
  - 双向 DC/DC 通信协议（CAN/RS485、报文 ID/寄存器、字节序、缩放单位）。
  - DC/DC 启停、充电、放电、目标电流/电压/功率和故障复位命令定义。
  - 市电及 AC/DC 在线/故障状态的采集方式。
  - 直流母线额定电压、欠压/过压阈值和切换允许范围。
  - AC/DC 是否支持功率调节，以及市电恢复时 DC/DC 退出备电的时序要求。
- 取得协议后建议按以下顺序推进：
  1. 新建独立 `dcdc_driver`，先完成状态接收和通信超时判断。
  2. 扩展 EMS 输入快照，加入电源侧和母线侧数据。
  3. 将 EMS 输出替换为 `ems_dcdc_target_t`，策略层不直接发送 CAN/Modbus。
  4. 增加 `EMS_Task`，周期建议先采用 1s；DC/DC 协议任务按设备要求采用更快周期。
  5. 依次验证正常充电、削峰放电、市电掉电备电、市电恢复、BMS/DC-DC 离线和保护锁定。

记录时间：2026-07-16（Asia/Shanghai）
