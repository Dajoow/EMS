# 基站EMS工程修改方案

## 1. 目标定位

本方案以当前 `BSMU_H750IB` 工程为基础，打造新的基站 EMS 工程。目标不是简单把现有 BSMU 数据上传程序改名，而是在保留现有 BCMU/BMU 数据采集能力的基础上，补齐 PCS 控制、EMS 策略、云平台闭环、保护联锁和工程化配置能力。

目标系统需要实现：

- 监控 BCMU 通过 FDCAN 上传的 2 组 BMU 数据。
- 每组 BMU 管理 12 节 2V 电池，即单个 BCMU 对应 24 节 2V 电池。
- 基于电池电压、电流、SOC、SOH、温度、绝缘、告警等级，对 PCS 模块执行充放电控制策略。
- 将站级、簇级、BMU、电芯、PCS、EMS 策略状态上传云平台。
- 支持本地调试、Modbus/RS485读写、TouchGFX显示、云端参数下发与控制确认。
- 形成可长期维护的新 EMS 工程，而不是在业务代码中继续堆叠临时逻辑。

## 2. 当前工程可复用能力

当前工程已经具备较完整的 BSMU 数据采集与上报基础，建议作为新 EMS 的底座复用。

### 2.1 启动与RTOS框架

- `Core/Src/main.c` 完成芯片启动、Cache、MPU、EEPROM读取、FDCAN2、FMC、LTDC、UART、RTC、mbedTLS、TouchGFX 等初始化。
- `Core/Src/freertos.c` 中 `Startup()` 启动 `StationDataInit()`、EasyFlash、SNTP、`BSMU_CANInit()`、4G模块、屏幕休眠、Modbus、本地TCP、HTTPS云端和CPU监控任务。
- 可以复用 FreeRTOS 任务模型，但建议新 EMS 业务增加独立 `ems_task`，不要把策略逻辑塞进 `CAN_Poll()` 或 `httpc_task()`。

### 2.2 BCMU/BMU采集基础

- 当前使用 `FDCAN2` 与 BCMU 通信。
- 现有 BCMU ID 为 `0x101` 到 `0x114`，最大支持 20 个 BCMU。
- `Core/Src/CAN_Control.c` 已实现 FDCAN FD 64字节收发、分包、CRC16(Modbus)校验、BCMU轮询和在线离线判断。
- `Core/Inc/station_ctl.h` 当前数据模型为 `cluster_num=20`、`GRP_num=30`、`GRP_BAT_num=12`，即原设计为每簇最多 30 组、每组 12 节。

### 2.3 数据聚合与告警基础

- `Core/Src/station_ctl.c` 已计算站级电压、电流、SOC、SOH、充电功率、放电功率、可充容量、可放容量、最大/最小单体电压、最大/最小温度等。
- 已具备簇级 warn、alarm、protect 分级告警逻辑。
- 已有充放电能力估算雏形，例如单体电压 `2.45V` 前后切换充电阶段电流。

### 2.4 本地与云端上报基础

- `Core/Src/client.c` 已实现本地 TCP 客户端上报，发送在线簇数据和站级数据。
- `Core/Src/http_client.c` 已实现 HTTPS POST 云端上报，接口包括：
  - `/api/bmsRequest/sendSum`
  - `/api/bmsRequest/clusterData`
- 现有云端方向主要是“上传”，缺少云端控制下发、命令确认、超时回退、策略审计。

### 2.5 Modbus/RS485基础

- `Drivers/BSP/Components/RS485/modbus_data.*` 已有站级、簇级、单体数据寄存器映射。
- 当前已有 `station_charge_ctrl.charge_ctrl`，但它更像 BCMU 开关控制，不是完整 PCS 充放电控制。

## 3. 当前工程主要缺口

要打造新的 EMS 工程，当前至少缺少以下核心模块：

- PCS CAN 协议层：没有独立 PCS CAN ID、状态解析、故障解析、启停命令、功率/电流设定、心跳、复位逻辑。
- EMS 策略层：没有完整状态机，没有把 BCMU/BMU、PCS、云端、本地控制和保护联锁统一决策。
- 云端控制闭环：现有云端任务主要上报，缺少下发命令、执行确认、失败原因回传。
- 参数管理：PCS额定参数、SOC上下限、充放电限值、策略模式、云端凭据仍需要配置化。
- 数据模型适配：目标是每个 BCMU 只上传 2 组 BMU，每组 12 节；现工程最大按 30 组设计，需要做运行时裁剪，而不是粗暴改常量。
- 安全机制：需要明确保护优先级、故障锁定、PCS停机、继电器/接触器状态校验、通信丢失降级策略。

## 4. 三个最优修改方案

下面给出三个可落地的最优修改方案。三者不是互斥关系，可以按阶段组合实施。

## 方案一：最小风险渐进式改造方案

### 方案定位

在现有工程上最小改动，优先复用 FDCAN2 采集、站级聚合、Modbus、云端上传，新增 EMS 策略模块和 PCS 控制模块。适合第一版样机、硬件接口未完全冻结、需要尽快跑通 EMS 基本闭环的阶段。

### 核心做法

- 保留当前工程启动链路和任务模型。
- 新增 `Core/Inc/ems_strategy.h`、`Core/Src/ems_strategy.c`，独立实现 EMS 策略状态机。
- 新增 `Core/Inc/pcs_can.h`、`Core/Src/pcs_can.c`，封装 PCS CAN 协议。
- 在 `Startup()` 中增加 `EMS_Init()`，不要直接把策略逻辑写入 `CAN_Poll()`。
- 在 `CAN_Poll()` 完成 BCMU 一轮轮询、`CalStationData()` 和 `cal_modbus_data()` 后，通过事件或信号量唤醒 `ems_task`。
- PCS 控制先只支持手动启停、充电电流限制、放电电流限制、故障停机，后续再加自动策略。

### EMS状态机建议

- `EMS_STATE_INIT`：等待 BCMU、PCS、网络、配置就绪。
- `EMS_STATE_STANDBY`：系统待机，允许云端/本地下发模式。
- `EMS_STATE_CHARGE`：充电运行。
- `EMS_STATE_DISCHARGE`：放电运行。
- `EMS_STATE_STOPPING`：执行PCS停机、BCMU开关断开等动作。
- `EMS_STATE_FAULT_LOCK`：保护级故障锁定，必须复位后恢复。
- `EMS_STATE_REMOTE_CTRL`：云端接管策略。

### 优点

- 改动范围小，最容易基于现有工程验证。
- 复用当前 BMS/BCMU 数据链路，开发周期最短。
- 适合快速做第一版 EMS 样机。

### 缺点

- 当前工程历史包袱仍然存在，例如部分硬编码地址、云端 Token 明文、结构体布局强耦合。
- 若 PCS 与 BCMU 共用 FDCAN2，总线调度和协议隔离需要非常小心。
- 长期维护性不如重新分层后的工程。

### 适用场景

- 第一阶段样机验证。
- PCS协议还在调试。
- 云平台接口暂不复杂。
- 需要尽快形成“采集-决策-控制-上传”的闭环。

## 方案二：双CAN/分层架构方案

### 方案定位

将 BCMU/BMU 采集总线和 PCS 控制总线分离，形成更清晰的新 EMS 架构。推荐作为正式产品化方向。如果硬件支持 FDCAN1/FDCAN2 或外扩 CAN，该方案最稳。

### 核心做法

- FDCAN2 保留为 BCMU/BMU 数据采集总线。
- FDCAN1 或外扩 CAN 控制器作为 PCS 控制总线。
- 建立分层目录结构：
  - `App/EMS/`：EMS状态机、策略计算、限流降额、故障闭锁。
  - `App/BMS/`：BCMU/BMU数据适配、2组BMU裁剪、数据有效性判断。
  - `App/PCS/`：PCS协议、状态解析、命令发送、心跳、故障复位。
  - `App/Cloud/`：云端上传、命令下发、命令确认。
  - `App/Config/`：参数默认值、EEPROM/EasyFlash持久化、配置校验。
- 对现有 `CAN_Control.c` 做“BCMU总线驱动”定位，不再承担 EMS 控制职责。
- 对 PCS 新建独立周期任务，例如 `pcs_task`：周期读取 PCS 状态，执行 EMS 下发的目标功率/电流/启停命令。
- EMS策略任务只读 BMS/PCS 状态快照，输出控制意图，不直接操作底层 CAN。

### 推荐任务模型

- `bcmu_poll_task`：轮询 BCMU，更新 BMS 数据快照。
- `pcs_task`：维护 PCS 通信状态，执行控制命令。
- `ems_task`：每 200ms 或 1s 计算策略，输出目标控制量。
- `cloud_task`：上传遥测，接收云端命令。
- `modbus_task`：本地EMS寄存器读写。
- `persist_task`：周期保存累计量和配置。

### 优点

- BCMU采集和PCS控制互不干扰，实时性和故障隔离更好。
- 工程分层清晰，适合长期维护和产品化。
- 后续支持多型号PCS时，只需替换 `App/PCS/` 适配层。
- EMS策略可以做单元测试或仿真，不依赖底层CAN细节。

### 缺点

- 初期工作量较大。
- 需要确认硬件是否保留 FDCAN1 引脚和收发器，或是否允许外扩 CAN。
- 需要重新梳理 Keil 工程文件、include path、链接脚本/section。

### 适用场景

- 目标是正式产品。
- 需要长期维护多个站点或多个PCS型号。
- 对控制可靠性、总线负载、故障隔离有较高要求。

## 方案三：云边协同EMS方案

### 方案定位

把本地 EMS 作为安全闭环和实时控制核心，把云平台作为策略配置、远程调度、数据分析和运维入口。适合基站EMS需要远程调度、削峰填谷、备电策略、远程升级和集中运维的场景。

### 核心做法

- 本地 EMS 永远掌握安全控制权，云端只下发目标和模式，不能绕过本地保护。
- 云端下发字段建议包括：
  - 运行模式：待机、充电、放电、自动、维护。
  - 目标功率或目标电流。
  - 允许充电SOC上限。
  - 允许放电SOC下限。
  - 策略生效时间窗。
  - 命令序号和超时时间。
- 本地 EMS 执行命令后上传确认：
  - 命令已接收。
  - 命令已执行。
  - 命令被拒绝。
  - 拒绝原因，例如保护告警、PCS离线、BCMU离线、SOC越限、温度越限。
- 云端上传数据扩展：
  - PCS状态、电压、电流、功率、故障字。
  - EMS状态机状态。
  - 目标功率、电池允许充放电功率、实际PCS功率。
  - 降额原因、保护原因、通信质量。

### 云端接口建议

保留现有上传接口作为兼容层，新增以下逻辑接口：

- `POST /api/ems/telemetry`：EMS综合遥测。
- `GET /api/ems/command?deviceId=...`：拉取云端命令。
- `POST /api/ems/commandAck`：命令执行确认。
- `POST /api/ems/fault`：故障和保护事件上报。
- `POST /api/ems/configReport`：本地配置上报。

### 优点

- 更符合“基站EMS”的真实使用方式。
- 支持远程策略、远程运维和数据闭环。
- 可以把云端调度和本地安全边界分清楚，降低误控制风险。

### 缺点

- 云端接口、鉴权、命令重试、断网缓存都需要补充。
- HTTPS线程需要从单纯上传改造成遥测+命令双向交互。
- 对网络稳定性和时间同步要求更高。

### 适用场景

- 基站需要远程调度充放电。
- 需要云平台统一管理多个EMS站点。
- 后续有削峰填谷、备电、电价策略、告警运维等需求。

## 5. 推荐组合路线

综合当前工程状态和目标，推荐采用“方案一快速闭环 + 方案二产品化分层 + 方案三云端增强”的组合路线。

### 阶段一：EMS最小闭环

目标：在现有工程上快速形成可运行 EMS 样机。

建议工作：

- 新增 EMS 状态机任务。
- 新增 PCS CAN 协议模块。
- 保留 FDCAN2 BCMU采集逻辑。
- 将 2组BMU作为运行时有效组数处理，不先大改 `GRP_num=30`。
- 增加基本保护闭锁：单体过压、单体欠压、温度越限、绝缘低、BCMU离线、PCS离线。
- 增加手动充电、手动放电、停止、复位四类控制。

验收标准：

- 能稳定采集 2组BMU、24节电池数据。
- 能计算允许充电电流和允许放电电流。
- 能控制 PCS 启停和功率/电流设定。
- 保护触发时 PCS 停止输出并进入故障锁定。
- 本地串口/Modbus/云端能看到 EMS 状态。

### 阶段二：正式EMS工程分层

目标：把第一阶段样机整理成可维护的新 EMS 工程。

建议工作：

- 建立 `App/EMS`、`App/BMS`、`App/PCS`、`App/Cloud`、`App/Config` 分层。
- 把 BCMU 数据转换为标准 `bms_snapshot_t`。
- 把 PCS 数据转换为标准 `pcs_snapshot_t`。
- EMS策略只处理快照和目标，不依赖具体CAN帧。
- 整理配置持久化结构，加入版本号和CRC。
- 清理云端 Token、服务器地址、PCS参数硬编码。

验收标准：

- 策略层和协议层解耦。
- 支持替换PCS协议适配文件。
- 配置升级不破坏旧参数。
- 代码结构清晰，新工程命名和职责边界明确。

### 阶段三：云边协同与运维能力

目标：让 EMS 具备远程控制、策略下发、故障审计和长期运维能力。

建议工作：

- 增加云端命令拉取与确认机制。
- 增加命令序号、时间戳、超时失效。
- 增加断网缓存和重连补报。
- 增加故障事件上报。
- 增加 EMS 策略参数云端配置。
- 强化 TLS 证书校验和鉴权。

验收标准：

- 云端可以下发充电/放电/停止/自动策略。
- 本地EMS能拒绝危险命令并上传原因。
- 断网后本地保护和控制仍然安全运行。
- 网络恢复后能补报关键事件。

## 6. 关键数据模型建议

### 6.1 BMS快照

建议新增统一快照，避免 EMS 直接读 `Client_Sd` 原始结构体。

```c
typedef struct {
    uint8_t online;
    uint16_t cluster_id;
    float cluster_voltage;
    float cluster_current;
    float cluster_soc;
    float cluster_soh;
    float max_cell_voltage;
    float min_cell_voltage;
    float max_cell_temp;
    float min_cell_temp;
    float pos_insulation_res;
    float neg_insulation_res;
    uint32_t warning_bits;
    uint32_t alarm_bits;
    uint32_t protect_bits;
} bms_cluster_snapshot_t;
```

### 6.2 PCS快照

```c
typedef struct {
    uint8_t online;
    uint8_t running;
    uint8_t mode;
    float dc_voltage;
    float dc_current;
    float active_power;
    uint32_t fault_bits;
    uint32_t warning_bits;
    uint32_t last_rx_tick;
} pcs_snapshot_t;
```

### 6.3 EMS控制目标

```c
typedef struct {
    uint8_t enable;
    uint8_t mode;
    float target_power_kw;
    float charge_current_limit_a;
    float discharge_current_limit_a;
    uint16_t command_seq;
    uint32_t expire_tick;
} ems_control_target_t;
```

## 7. 充放电策略建议

### 7.1 充电允许条件

全部满足才允许充电：

- PCS在线且无严重故障。
- 至少一个 BCMU 在线。
- 无 protect 级告警。
- 单体最高电压低于充电保护阈值。
- 最高温度低于充电温度保护阈值。
- 最低温度高于低温充电保护阈值。
- SOC低于充电上限。
- 绝缘电阻满足安全阈值。

### 7.2 充电限流策略

建议第一版采用分段策略：

- 最高单体电压低于 `2.40V`：允许较大充电电流。
- 最高单体电压在 `2.40V` 到 `2.45V`：线性降额。
- 最高单体电压高于 `2.45V`：进入小电流浮充或停止充电。
- 任一告警达到 alarm：降额。
- 任一保护达到 protect：停止充电并锁定。

### 7.3 放电允许条件

全部满足才允许放电：

- PCS在线且无严重故障。
- 至少一个 BCMU 在线。
- 无 protect 级告警。
- 单体最低电压高于放电保护阈值。
- SOC高于放电下限。
- 放电温度范围正常。
- 绝缘电阻满足安全阈值。

### 7.4 放电限流策略

建议第一版采用：

- SOC高于正常区间：允许额定放电。
- SOC接近下限：按SOC线性降额。
- 单体最低电压接近欠压阈值：按电压线性降额。
- 温度接近保护阈值：按温度降额。
- 触发保护：停止放电并锁定。

## 8. Modbus寄存器扩展建议

现有 `station_charge_ctrl` 只有一个控制字，不足以表达 EMS 控制。建议扩展为：

- `ems_mode`：0待机，1手动充电，2手动放电，3自动，4维护。
- `ems_cmd`：0无，1启动，2停止，3复位，4急停。
- `target_power_kw`：目标功率。
- `charge_current_limit_a`：充电电流上限。
- `discharge_current_limit_a`：放电电流上限。
- `soc_charge_stop`：充电停止SOC。
- `soc_discharge_stop`：放电停止SOC。
- `ems_state`：当前EMS状态。
- `ems_fault_code`：EMS故障码。
- `derating_reason`：降额原因。
- `pcs_online`：PCS在线状态。
- `pcs_fault_bits`：PCS故障字。

## 9. 云平台数据扩展建议

现有云端仅上传站级和簇级 BMS 数据。新的 EMS 工程建议上传：

- 站级BMS数据：电压、电流、SOC、SOH、最大/最小单体、告警状态。
- 2组BMU数据：每组12节电压、温度、SOC、SOH、均衡状态。
- PCS数据：运行状态、模式、DC电压、DC电流、有功功率、故障字。
- EMS数据：状态机、目标功率、允许充电电流、允许放电电流、降额原因、控制来源。
- 事件数据：保护触发、PCS故障、BCMU离线、云端命令拒绝、参数变更。

## 10. 风险与必要修复点

打造新 EMS 前，建议优先处理这些已发现风险：

- `CalStationData()` 在线 BCMU 数为 0 时可能除以 0。
- `cal_modbus_cluster_data()` 有效电芯数量为 0 时可能除以 0。
- 温度告警部分代码存在疑似数组索引问题，应检查 `cell_max_temp->val` 是否应为 `cell_max_temp[i].val`。
- `SERVER_TOKEN` 明文硬编码在 `http_client.c`，正式工程应迁移到安全配置区。
- TLS证书校验策略需要上线前收紧。
- 当前 EEPROM 配置结构没有版本号和CRC，后续字段扩展容易兼容失败。
- 固定SDRAM地址和 section 强相关，新增大缓冲区前必须复核链接脚本和MPU/Cache策略。
- FDCAN2 当前承担 BCMU 轮询，若 PCS 共线，需要先计算总线负载和优先级。

## 11. 推荐最终选择

如果目标是“打造一个新的EMS工程”，推荐最终采用方案二作为主架构，方案一作为过渡实现，方案三作为云平台增强方向。

推荐决策：

- 样机阶段：采用方案一，最快闭环。
- 产品阶段：升级到方案二，完成分层和双CAN隔离。
- 平台阶段：叠加方案三，实现云边协同和远程运维。

最终工程应形成：

```text
BSMU_H750IB / Hdu_EMS_H750
├── Core
├── Drivers
├── Middlewares
├── App
│   ├── EMS
│   ├── BMS
│   ├── PCS
│   ├── Cloud
│   └── Config
├── TouchGFX
└── Docs
```

其中：

- `Core` 保留 CubeMX 生成和基础启动代码。
- `Drivers` 保留底层驱动。
- `App/BMS` 负责 BCMU/BMU 数据适配。
- `App/PCS` 负责 PCS 协议和执行。
- `App/EMS` 负责策略和状态机。
- `App/Cloud` 负责遥测、命令和确认。
- `App/Config` 负责配置、默认值、版本迁移。
- `Docs` 保存协议、寄存器表、云端接口、测试方案。

## 12. 第一版开发清单

建议第一版按以下顺序开发：

1. 明确 PCS CAN 协议文档，包括 ID、周期、字节序、单位、故障字。
2. 新建 PCS 协议模块，只接收 PCS 状态，不立即控制。
3. 新建 EMS 状态机，先只输出日志和 Modbus 状态。
4. 适配 2组BMU数据，只处理每个 BCMU 的前 24 节电池。
5. 实现手动停止、手动充电、手动放电、故障复位。
6. 实现充放电限流和保护闭锁。
7. 扩展 Modbus 寄存器，便于本地调试。
8. 扩展云端遥测，上传 PCS 和 EMS 状态。
9. 增加云端命令拉取、执行确认和拒绝原因。
10. 做断网、PCS离线、BCMU离线、保护触发、恢复复位测试。

## 13. 验收建议

第一版 EMS 工程建议至少通过以下测试：

- BCMU在线/离线测试。
- 2组BMU、24节电池数据正确性测试。
- PCS在线/离线测试。
- 充电启动、充电停止、放电启动、放电停止测试。
- 单体过压保护测试。
- 单体欠压保护测试。
- 高温/低温保护测试。
- 绝缘低保护测试。
- 云端命令超时测试。
- 云端命令被保护拒绝测试。
- Modbus本地控制与云端控制冲突测试。
- 断网后本地EMS继续安全运行测试。

## 14. 总结

当前工程适合作为新 EMS 的底层基础，但不建议继续把 PCS 控制和云端下发逻辑直接混入现有 `CAN_Control.c` 或 `http_client.c`。最佳路线是先用最小改造跑通闭环，再逐步重构为 BMS、PCS、EMS、Cloud、Config 分层架构。这样既能快速推进样机，又能避免后期工程变成难维护的“毛线团”。
